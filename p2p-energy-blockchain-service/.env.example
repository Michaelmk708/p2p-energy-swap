# API key that AI service and other internal services will use to talk to this microservice
BLOCKCHAIN_SERVICE_API_KEY=replace-with-secure-key

# Path to oracle keypair (for Anchor / Solana signer)
ORACLE_KEYPAIR_PATH=/path/to/oracle/keypair.json

# Solana cluster URL (RPC)
SOLANA_RPC_URL=https://api.devnet.solana.com
