## Backend API base for local development
## Use the local Django server by default. This avoids ngrok host/CORS issues when developing.
VITE_API_BASE=http://127.0.0.1:8000/api

## If you want to expose the backend over ngrok for external testing,
## set VITE_API_BASE_URL to your ngrok URL. Comment it out for local-only.
# VITE_API_BASE_URL=https://<your-ngrok-subdomain>.ngrok-free.dev/api
