# Frontend (Vite) environment for IoT Central integration
# Device ID to poll from the backend latest endpoint
VITE_IOTC_DEVICE_ID=sim-1

# Optional: component name if your telemetry is under a component (e.g., inverter)
# Leave empty to use root-level telemetry
VITE_IOTC_COMPONENT=
