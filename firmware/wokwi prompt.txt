f=open('main.py','w'); f.write("from machine import Pin, ADC\nimport network, time, ujson, usocket\n\npv = ADC(Pin(35))\npv.atten(3)\nload = ADC(Pin(34))\nload.atten(3)\n\nhost = '192.168.0.111'\nport = 8000\n\ndef send_data(pv_kw, load_kw):\n try:\n  payload1 = ujson.dumps({'household_id': 'sim-1', 'pv_power': pv_kw, 'load_power': load_kw, 'timestamp': time.time()}).encode()\n  addr = usocket.getaddrinfo(host, port)[0][-1]\n  s = usocket.socket()\n  s.settimeout(5)\n  s.connect(addr)\n  req = b'POST /api/energy-reading/ HTTP/1.0\\r\\nHost: localhost\\r\\nContent-Type: application/json\\r\\nContent-Length: ' + str(len(payload1)).encode() + b'\\r\\n\\r\\n'\n  s.write(req + payload1)\n  resp1 = s.recv(1024)\n  s.close()\n  payload2 = ujson.dumps({'device': 'sim-1', 'component': 'pv_array', 'data': {'power': pv_kw * 1000}}).encode()\n  addr = usocket.getaddrinfo(host, port)[0][-1]\n  s = usocket.socket()\n  s.settimeout(5)\n  s.connect(addr)\n  req = b'POST /api/iotcentral/telemetry_bridge/ HTTP/1.0\\r\\nHost: localhost\\r\\nContent-Type: application/json\\r\\nContent-Length: ' + str(len(payload2)).encode() + b'\\r\\n\\r\\n'\n  s.write(req + payload2)\n  resp2 = s.recv(1024)\n  s.close()\n  payload3 = ujson.dumps({'device': 'sim-1', 'component': 'house_load', 'data': {'power': load_kw * 1000}}).encode()\n  addr = usocket.getaddrinfo(host, port)[0][-1]\n  s = usocket.socket()\n  s.settimeout(5)\n  s.connect(addr)\n  req = b'POST /api/iotcentral/telemetry_bridge/ HTTP/1.0\\r\\nHost: localhost\\r\\nContent-Type: application/json\\r\\nContent-Length: ' + str(len(payload3)).encode() + b'\\r\\n\\r\\n'\n  s.write(req + payload3)\n  resp3 = s.recv(1024)\n  s.close()\n  if (b'200' in resp1 or b'201' in resp1) and (b'200' in resp2 or b'201' in resp2) and (b'200' in resp3 or b'201' in resp3):\n   return '[OK]'\n  else:\n   return '[OK*]'\n except Exception as e:\n  return '[' + str(type(e).__name__) + ']'\n\nw = network.WLAN(network.STA_IF)\nw.active(True)\nw.connect('Wokwi-GUEST', '')\nwhile not w.isconnected():\n time.sleep(0.5)\nprint('WiFi OK')\n\nit = 0\nwhile True:\n it += 1\n pv_raw = pv.read()\n load_raw = load.read()\n pv_kw = round(pv_raw * 10000 / 4095 / 1000.0, 3)\n load_kw = round(load_raw * 5000 / 4095 / 1000.0, 3)\n status = send_data(pv_kw, load_kw)\n print(f'[{it}] PV={pv_kw}kW Load={load_kw}kW {status}')\n time.sleep(2)\n"); f.close()





import main
